name: my first pipeline

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: uptime
        run: uptime

      - name: number of processors
        run: nproc

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build image
        run: docker build -t Devotha123/Portfolio-app1 .

      - name: List images
        run: docker images






         